server:
  port: 8081

spring:
  application:
    name: service-a

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

sample:
  security:
    service-b-uri: http://localhost:8082

chenile:
  security:
    jwt:
      issuer-base: http://localhost:9000
      jwk-base-uri: http://localhost:9000
